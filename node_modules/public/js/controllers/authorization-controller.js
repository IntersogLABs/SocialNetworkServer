define([
  'controllers/base/controller',
  'views/authorization/authorization-view'
], function(Controller, AuthorizationView) {
  'use strict';

  var AuthorizationController = Controller.extend({
    initialize: function(){

    },

    index: function(params) {
      this.view = new AuthorizationView({
        region: 'sidebar'
      });
    }
  });

  return AuthorizationController;
});
