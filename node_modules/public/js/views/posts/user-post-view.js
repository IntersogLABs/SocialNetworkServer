define([
  'views/base/view',
  'text!templates/posts/user-post-view.hbs'
], function(View, template) {
  'use strict';

  var UserPostItemView = View.extend({
    autoRender: true,

    template: template
  });

  return UserPostItemView;
});