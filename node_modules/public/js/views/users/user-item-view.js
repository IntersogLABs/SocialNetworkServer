define([
  'views/base/view',
  'text!templates/users/user-item-view.hbs'
], function(View, template) {
  'use strict';

  var UserItemView = View.extend({
    autoRender: true,

    template: template
  });

  return UserItemView;
});
