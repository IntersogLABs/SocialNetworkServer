define([
  'views/base/view',
  'text!templates/feed/feed-view.hbs'
], function(View, template) {
  'use strict';

  var FeedView = View.extend({
    autoRender: true,
    
    template: template
  });

  return FeedView;
});