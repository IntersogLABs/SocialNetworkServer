define([
  'views/base/view',
  'text!templates/follow/follow-view.hbs'
], function(View, template) {
  'use strict';

  var FollowView = View.extend({
    autoRender: true,

    template: template
  });

  return FollowView;
});