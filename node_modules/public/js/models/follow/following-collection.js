define([
  'models/base/collection',
  './following-model'
], function(Collection, FollowingModel) {
  'use strict';

  var FollowingCollection = Collection.extend({
    url: config.apiUrl + "me/following",
    model: FollowingModel
  });

  return FollowingCollection;
});