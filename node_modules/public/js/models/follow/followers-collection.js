define([
  'models/base/collection',
  './followers-model'
], function(Collection, FollowersModel) {
  'use strict';

  var FollowersCollection = Collection.extend({
    url: config.apiUrl + "me/followers",
    model: FollowersModel
  });

  return FollowersCollection;
});