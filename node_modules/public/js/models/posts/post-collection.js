define([
  'models/base/collection',
  './post-model'
], function(Collection, PostModel) {
  'use strict';

  var Post = Collection.extend({
    url:config.apiUrl + "me/wall",
    model: PostModel
  });

  return Post;
});